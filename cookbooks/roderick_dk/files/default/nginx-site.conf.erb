server {
  listen   80;
  server_name  <%= node[:hostname] %>;

  access_log  <%= node[:nginx][:log_dir] %>/<%= node[:hostname] %>.access.log;

  location / {
    root   /vagrant/site/_site;
    index  index.html;
  }
}
